generated: '2026-07-24'
method: derived
source: mcp/human-api-mcp.yml (candidate) + openapi/*.json
note: >-
  Crosswalk binding the derived MCP candidate tools to their backing OpenAPI operations.
  There is no official hosted MCP server and no GraphQL surface; the MCP column is a
  candidate projection of the REST Admin API, so every binding is high-confidence by
  construction. rest_only[] lists harvested operations not surfaced as a candidate tool.
surfaces:
  openapi: openapi/*.json (per-endpoint OpenAPI 3.0.3, ReadMe-generated)
  graphql: null
  mcp:
    url: null
    status: candidate
    gated: false
crosswalk:
- tool: get_order_types
  category: orders
  rest: [getOrderTypes]
  binding: rest
  confidence: high
- tool: list_users
  category: users
  rest: [getUsersList]
  binding: rest
  confidence: high
- tool: get_user_reports
  category: reports
  rest: [getUserReports]
  binding: rest
  confidence: high
mcp_only: []
rest_only:
- operationId: post_v1-admin-token
  capability: auth
  note: Admin API token exchange (POST /v1/admin/token); not exposed as a tool.
- operationId: post_v1-connect-token
  capability: auth
  note: Connect token exchange (POST /v1/connect/token); not exposed as a tool.
coverage:
  tools_named: 3
  tools_bound: 3
  mcp_only: 0
  rest_ops_total: 5
  rest_ops_with_tool: 3
